[build]
builder = "nixpacks"
buildCommand = "npm ci && npx prisma generate && npm run build"

[deploy]
startCommand = "sh -c 'echo \"[Migration] Starting database migrations...\" && npx prisma migrate deploy && echo \"[Migration] Migrations completed successfully\" && echo \"[Server] Starting server...\" && npm run start'"

